<h1 class= "title">Admin</h1>
<div class="container">

  <% if user_signed_in? && current_user.admin? %>

    <div class="admin-links">
      <a class="admin-link" href="<%=commandes_path%>"><h2><i class="fa-solid fa-list"></i> Mes Commandes</h2></a>
      <a class="admin-link" href="<%=catalogue_path%>"><h2><i class="fa-solid fa-book"></i> Mon Catalogue</h2></a>
      <a class="admin-link" href="<%=livraison_path%>"><h2><i class="fa-solid fa-truck"></i> Zone de Livraison</h2></a>
    </div>
<br>
    <div class="open-shop">
    <h2 class="text-center"><i class="fa-solid fa-store"></i> Ouvrir / Fermer la Boutique</h2>
      <%= simple_form_for @shop_status do |f| %>
        <%= f.input :active, as: :boolean, label: 'Boutique ouverte ?', class: 'form-control' %>
        <%= f.input :message, label: 'Message :', class: 'form-control' %>
        <%= f.submit 'Mettre à jour', class: 'btn-action' %>
      <% end %>
    </div>
  <% else %>
  <p class="text-center align-middle">Vous n'avez pas accès à cette page. 🤷‍♂️</p>
  <% end %>

</div>
